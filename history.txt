#######################################################
2026.03.26 | Ver 1.0  | 최초커밋
#######################################################
1. 소스 생성
    거의 완벽하게 작동했어요! 🎉 3시간짜리 영상(2.9GB)이랑 썸네일까지 만들어졌어요!
2. 미결사항
    마지막에 metadata.json 저장할 때 Windows 인코딩 문제(cp949)만 fix하면 돼요. 아주 간단해요. pipeline.py 에서 한 줄만 수정

#######################################################
2026.03.28 | Ver 1.1  | 업데이트 내용
#######################################################
1. 중복 소스 스킵 (freesound.py, pexels.py)
    프로젝트 루트에 used_assets.json 자동 생성.
    다운로드 완료된 소스 ID를 기록해두고, 다음 실행 때 자동 스킵.
    로그에 3 skipped (used) / 7 fresh 이런 식으로 표시됨.

2. 로고 삽입 (ffmpeg_producer.py, thumbnail.py)
    영상: 우측 하단 반투명(60%) 워터마크.
    썸네일: 우측 하단 반투명(80%) 로고.

3. 한글 지원 (pipeline.py, thumbnail.py)
    맑은 고딕 폰트 자동 로드 (Windows 기본 내장).
    한글 설명/태그/더보기 텍스트 자동 생성.

4. 로컬 음원 폴백: assets/sounds/{category}/ 폴더 파일 우선 사용(Freesound 시스템 점검 시 보완로직)
    사운드 수집 우선순위:
        1. assets/sounds/{category}/ 폴더에 파일 있으면 → 로컬 파일 우선 사용
        2. 로컬 파일 없거나 부족하면 → Freesound API로 자동 수집
        3. Freesound도 다운된 경우 → 로컬 파일만으로 진행 (없으면 실패)
    freesound.py에 LocalSoundCollector 클래스 추가
    동작순서:
        (as-is) python pipeline.py 실행
        (to-be) assets/sounds/rain/ 폴더 확인
          ├─ 파일 있음 → 로컬 파일 사용 (API 호출 없음)
          ├─ 파일 부족 → Freesound API로 나머지 채우기
          └─ Freesound 다운 → 로컬 파일만으로 진행

#######################################################
2026.03.29 | Ver 1.2  | 업데이트 내용
#######################################################
1. 워터마크, 썸네일 수정
    변경 내용:
    1-1) thumbnail.py
        generate() 에 video_path 파라미터 추가 → 해당 영상 첫 프레임 자동 추출해서 배경으로 사용
        배경 이미지 dominant color 기반으로 stroke 색상 자동 결정 (항상 배경과 어울리는 어두운 색조)
        title_sub, subtitle_en 파라미터로 썸네일 부제목 커스텀 가능

    1-2) pipeline.py
        video_files[0] 을 썸네일에 자동 전달
        concept에 title_sub, subtitle_en 키 추가
        duration_hours 1시간으로 변경

2. 임시 파일 단계별 즉시 삭제 → 디스크 사용량 최소화
    [디스크 사용 흐름]
      이전: 원본영상 + normalized + video_loop + mixed_audio + merged_no_logo + 최종 = 최종x3~4배
      이후: 원본영상 + mixed_audio(임시) + 최종 = 최종x1.1배 수준

    [임시 파일 관리 전략]
      - normalized 클립: 합성 직후 삭제
      - video_loop: merge 완료 직후 삭제
      - mixed_audio: merge 완료 직후 삭제
      - merged_no_logo: 로고 적용 완료 직후 삭제
      - temp 폴더: 파이프라인 완료 후 전체 삭제

3. 오디오 -14 LUFS 정규화 (YouTube 권장), 영상 좌상단 heading 로고 + 우하단 원형 로고 동시 삽입

4. 실 사용 파일만 output 적재  → 디스크 사용량 최소화
    (as-is):
        collect() → 파일 수집 → 바로 _used/ 이동 (사용 전에 이동!)
        output/sounds/ → 다운받은 것 전부 (5개 중 3개만 써도 5개 다 남음)

    (to-be):
      collect() → 파일 수집 (이동 X)
      produce() 완료 → 실제 사용한 파일만 _used/ 이동
      output/sounds/ → 실제 사용한 파일만 남기고 나머지 삭제
      output/videos/ → 실제 사용한 파일만 남기고 나머지 삭제

    Step 1: collect() — 파일 수집만, 이동 없음
    Step 2: collect() — 영상 수집만
    Step 3: produce() — FFmpeg으로 실제 사용
             ↓ 완료 후 _cleanup_assets() 호출
             ├─ 로컬 음원(assets/sounds/) → _used/{session_id}/ 이동
             ├─ output/sounds/ 미사용 파일 → 삭제
             └─ output/videos/ 미사용 파일 → 삭제
    Step 4: 썸네일 생성 (video_files[0] 이미 정리됐으면 안되니 썸네일이 먼저)

    판단 기준:
        sound_files, video_files = produce()에 실제로 넘긴 리스트 → 이것만 "사용한 파일"로 간주
        로컬 음원 여부: LOCAL_SOUNDS_DIR in sound_path.parents 로 판별 (API 다운로드 파일은 이동 안 함)

    [ffmpeg_producer.py]
        [디스크 사용 흐름]
          이전: 원본영상 + normalized + video_loop + mixed_audio + merged_no_logo + 최종 = 최종x3~4배
          이후: 원본영상 + mixed_audio(임시) + 최종 = 최종x1.1배 수준

        [임시 파일 관리 전략]
          - normalized 클립: 합성 직후 삭제
          - video_loop: merge 완료 직후 삭제
          - mixed_audio: merge 완료 직후 삭제
          - merged_no_logo: 로고 적용 완료 직후 삭제
          - temp 폴더: 파이프라인 완료 후 전체 삭제

5. output/{session_id}/pipeline.log 로그 파일 자동 생성

6. used_asset.json 포맷 변경
    e.g.)
    {
      "20260329_005810": {
        "title": "빗소리 ASMR | 1시간 숙면 & 집중 사운드",
        "created_at": "2026-03-29 00:58:10",
        "sounds": [
          "237729_Rain_and_Thunder_4.mp3",
          "344430_Light_Rain.mp3"
        ],
        "videos": [
          "pexels_5517022_2160p.mp4",
          "pexels_5197762_2160p.mp4",
          "pexels_15454042_2160p.mp4"
        ]
      },
      "20260329_130953": { ... }
    }
    session이 key라서 output 폴더명과 1:1 매칭되고, sounds/videos 파일명만 지우면 해당 소스 재사용 가능.
    **원하는 대로 쓰는 방법:**
    # 소스 재사용하고 싶으면
    → used_assets.json에서 해당 session 블록 전체 삭제
    → pipeline.py 재실행

    # 특정 음원만 재사용
    → 해당 session의 "sounds" 배열에서 파일명 삭제
    → assets/sounds/_used/{session_id}/ 에서 해당 파일을 원래 카테고리 폴더로 이동
    → pipeline.py 재실행

7. metadata.json에 실 사용파일 정보 추가
8. sound 수집 개수 판정로직 변경


#######################################################
 2026.03.29 | Ver 2.0  | 업데이트 내용
#######################################################
## ✅ Phase 1 — 영상 자동 제작(완료 26.3.29)
  - Freesound + Pexels 자동 수집 (로컬 폴백, 페이지네이션, used 추적, 중복 스킵)
  - FFmpeg 영상 제작 (사운드 레이어링, -14 LUFS, 로고 워터마크)
  - 썸네일 자동 생성 (영상 첫 프레임 배경, RIDIBatang + Bitter)
  - 파일 정리 (실제 사용 파일만, temp 즉시 삭제)
  - 로그 파일, metadata.json, used_assets.json

## ✅ Phase 2A — YouTube 자동 업로드(완료 26.3.29)
  - OAuth2 인증 + 토큰 자동 갱신
  - 영상 + 썸네일 업로드
  - 매일 오후 8시 KST 예약 공개
  - 로그/metadata에 YouTube URL 기록



#######################################################
 2026.03.29 | Phase 2B — AI 기획 자동화 (Claude Haiku)
#######################################################
리포(data 브랜치): used_assets.enc  ← 암호화된 파일 (내용 못 읽음)
GitHub Secrets:   ENCRYPTION_KEY    ← 복호화 키

[설정 순서]
0. .github 폴더 생성/ 파일 추가:
    AutomationCalmdromedaWithClaudeProject/
    ├── .github/          ← mkdir .github (파이참 터미널에서)
    │   └── workflows/    ← mkdir .github\workflows
    │       └── daily_pipeline.yml   ← 여기에 파일 넣기
    ├── pipeline.py
    ├── crypto_utils.py   ← 프로젝트 루트

1. 암호화 키 생성 (로컬에서 한 번만)
    pip install cryptography
    python crypto_utils.py --generate-key  → 출력된 키 복사해두기!! (매우 중요)

    1-1) ENCRYPTION_KEY 환경변수 설정 (생성한 키 값으로 교체)
        set ENCRYPTION_KEY=[생성키]
        cho %ENCRYPTION_KEY% (확인)

    1-2) .env 파일에 추가
        ENCRYPTION_KEY=여기에_생성한_키

    1-3) 파일 암호화
        python crypto_utils.py --encrypt used_assets.json
        python crypto_utils.py --encrypt history.txt

2. GitHub Secrets 등록 (브라우저)
    https://github.com/areumkang87/[리포지토리명]
    리포지토리 → Settings → Secrets and variables → Actions(클릭)→ New repository secret (아래 목록 등록)
    ```
    Security
    ├──Secrets and variables
    │   └── Actions   ← 여기 클릭
    ```
    [등록목록]
        Secret             | 이름값
        ENCRYPTION_KEY     : 위에서 생성한 키
        FREESOUND_API_KEY  : .env의 값
        PEXELS_API_KEY     : .env의 값
        ANTHROPIC_API_KEY  : .env의 값
        YOUTUBE_TOKEN_JSON : credentials/token.json 파일 전체 내용
        GOOGLE_CLIENT_SECRET_JSO : Ncredentials/client_secret.json 파일 전체 내용

3. data 브랜치 초기화 (파이참 터미널)
    # 작성전 현재버전 커밋 필수
        git add .
        git commit -m "작업내용 메모"
        # 그다음 브랜치 이동

    # 로컬에서 첫 암호화 & data 브랜치 생성
        git checkout --orphan data  ← 고아 브랜치로 이동
        (사용안함) git rm -rf .  ← 로컬 파일까지 전부 삭제 주의!!!! (master로 돌아와도 커밋 안 한 파일들은 이미 삭제됨)
        git add used_assets.enc history.enc
        git commit -m "data: init"
        git push origin data
        git checkout main

4. 테스트 실행
    GitHub 리포 → Actions → Daily Calmdromeda Pipeline → Run workflow 버튼으로 수동 실행 먼저 테스트!

Actions 실행 시작
  → data 브랜치에서 used_assets.enc 다운로드
  → ENCRYPTION_KEY로 복호화 → used_assets.json 생성
  → pipeline 실행
  → 완료 후 used_assets.json 다시 암호화
  → data 브랜치에 커밋 & 푸시

data 브랜치:
  used_assets.enc   ← used_assets.json 암호화
  history.enc       ← history.txt 암호화

GitHub Secrets:
  ENCRYPTION_KEY    ← 하나의 키로 둘 다 처리